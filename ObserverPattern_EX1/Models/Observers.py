def send_by_email(receipt):
    print(f'Sending by email receipt {str(receipt)}')


def save_on_database(receipt):
    print(f'Saving on database receipt {str(receipt)}')


def push_notification_on_slack(receipt):
    print(f'Notifying on slack receipt {str(receipt)}')
